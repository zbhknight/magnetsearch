A scrapy script to search torrentkitty for magnet links with a keyword.

### Features

* Terminal scripts
* Avoid the harassement of pop-up ads

### Requirements

```
pip install scrapy
```

### Run
```
cd magnetsearch && scrapy magnet crawl
```
